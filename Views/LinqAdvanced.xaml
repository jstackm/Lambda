<UserControl x:Class="Lambda.Views.LinqAdvanced"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Query"
                           Width="180"
                        Margin="10,10,10,5"/>
                <TextBlock Text="Lambda"
                           Width="180"
                        Margin="10,10,10,5"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="int Where"
                        Width="180"
                        HorizontalContentAlignment="left"
                        Margin="10,10,10,5"
                        Command="{Binding QueryIntWhere}"/>
                <Button Content="int Where"
                        Width="180"
                        HorizontalContentAlignment="left"
                        Margin="10,10,10,5"
                        Command="{Binding LambdaIntWhere}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="string Where"
                        Width="180"
                        HorizontalContentAlignment="left"
                        Margin="10,10,10,5"
                        Command="{Binding QueryStringWhere}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="Custom Class Where"
                        Width="180"
                        HorizontalContentAlignment="left"
                        Margin="10,10,10,5"
                        Command="{Binding QueryCustomClassWhere}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="Groupby"
                        Width="180"
                        HorizontalContentAlignment="left"
                        Margin="10,10,10,5"
                        Command="{Binding QueryGroupBy}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="Groupby Multi-Condition"
                        Width="180"
                        HorizontalContentAlignment="left"
                        Margin="10,10,10,5"
                        Command="{Binding QueryGroupByMulti}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="InnerJoin"
                        Width="180"
                        HorizontalContentAlignment="left"
                        Margin="10,10,10,5"
                        Command="{Binding QueryInnerJoin}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="InnerJoin Multi-Condition"
                        Width="180"
                        HorizontalContentAlignment="left"
                        Margin="10,10,10,5"
                        Command="{Binding QueryInnerJoinMulti}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="LeftJoin"
                        Width="180"
                        HorizontalContentAlignment="left"
                        Margin="10,10,10,5"
                        Command="{Binding QueryLeftJoin}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
